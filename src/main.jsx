import React from 'react'
import ReactDOM from 'react-dom/client'
import CalculadoraSATE from './CalculadoraSATE'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CalculadoraSATE />
  </React.StrictMode>,
)
